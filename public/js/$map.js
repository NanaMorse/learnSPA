define(function(require, exports, module){
    var $ = require('$');

    module.exports = {
        'msg' : {
            'msgIpt' : $('#msgIpt'),
            'sendMsgBtn' : $('#sendMsgBtn'),
            'msgList' : $('#msgList')
        }
    }
});